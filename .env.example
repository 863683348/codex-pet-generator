# ============================================
# Codex Pet Generator — Vercel 环境变量清单
# 在 Vercel 项目 → Settings → Environment Variables 中逐项填入
# 复制本文件内容到 Vercel，把 <...> 占位符换成真实值
# 真实值见本地 .env.local（不要提交到仓库！）
# ============================================

# ---------- Supabase（必填，真实生成链路依赖） ----------
# 从本地 .env.local 复制，以下为占位符示例
NEXT_PUBLIC_SUPABASE_URL=<你的 SUPABASE_URL>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<你的 SUPABASE_ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<你的 SUPABASE_SERVICE_ROLE_KEY>

# ---------- 图像生成 Provider（必填） ----------
# 可选值: bailian | openai
IMAGE_PROVIDER=bailian

# 百炼 / 通义千问（IMAGE_PROVIDER=bailian 时必填）
BAILIAN_API_KEY=<你的 BAILIAN_API_KEY>
BAILIAN_IMAGE_MODEL=qwen-image-2.0
BAILIAN_BASE_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation

# OpenAI（IMAGE_PROVIDER=openai 时填，本项目默认走百炼，留空即可）
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-1

# ---------- 应用 ----------
# SEO  canonical / sitemap / robots / manifest / OG 图都会用这个正式域名
# 例如：https://codex-pet-generator.vercel.app 或你的自定义域名
NEXT_PUBLIC_SITE_URL=<你的 Vercel 域名>

# 兼容性字段，旧代码可能仍引用，建议与 NEXT_PUBLIC_SITE_URL 保持一致
NEXT_PUBLIC_APP_URL=<你的 Vercel 域名>

# ---------- Google Search Console（可选）----------
# 在 GSC 添加站点后拿到验证码，填到这里即可生成 meta 验证标签
GOOGLE_SITE_VERIFICATION=<你的 google-site-verification 字符串>

# ---------- Google Analytics（可选，G- 开头的测量 ID） ----------
NEXT_PUBLIC_GA_MEASUREMENT_ID=<你的 Google Analytics 测量 ID，G- 开头>

# ---------- Creem 支付（可选，不用付费功能可留空） ----------
CREEM_API_KEY=
CREEM_PRO_PRICE_ID=
CREEM_UNLIMITED_PRICE_ID=
CREEM_WEBHOOK_SECRET=

# ---------- PayPal 支付（Live / 正式环境） ----------
# Live Client ID / Secret 来自 https://developer.paypal.com/dashboard/applications/live
# 如需切回沙盒：把下方值换成 sandbox 凭据，并将两个 *_PAYPAL_MODE 改回 sandbox
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
NEXT_PUBLIC_PAYPAL_MODE=live
PAYPAL_MODE=live
# 一次性购买月度额度的价格（USD），前端展示与后端下单一致
PAYPAL_PRO_PRICE=9
PAYPAL_UNLIMITED_PRICE=29
# Webhook（在 PayPal Live 后台注册或本地运行 scripts/register-paypal-webhook.mjs 后回填）
PAYPAL_WEBHOOK_ID=
PAYPAL_WEBHOOK_URL=https://codex-pet-generator.vercel.app/api/paypal/webhook
